{
  "name": "izc05-isivoltpro-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "verify": "node scripts/verify-build.mjs",
    "verify:identity": "node scripts/verify-platform-identity.mjs",
    "verify:portal": "node scripts/verify-platform-portal.mjs",
    "check": "npm run build && npm run verify && npm run verify:identity && npm run verify:portal",
    "preview": "astro preview --host 0.0.0.0",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "7.1.4"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
